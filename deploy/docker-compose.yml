services:
  corvo:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - corvo-data:/data
    restart: unless-stopped

volumes:
  corvo-data:
